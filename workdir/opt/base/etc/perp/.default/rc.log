#!/bin/rc

if (~ $#* 0) *=(start perpd)
if (! ~ $1 start ) exit

logdir=/var/log/$2

if (! test -d $logdir) {
	mkdir $logdir
	chown log:log $logdir
}

exec runuid log tinylog -k 8 -s 100000 -t -z $logdir

#!/bin/rc

if (~ $"1 start ) exit

logdir=/var/log/perpd

if (![ -d $"logdir ]) {
	mkdir "$logdir"
	chown -R log:log $logdir
	chmod 0755 $logdir
}

exec runuid log tinylog -k 8 -s 100000 -t -z $logdir

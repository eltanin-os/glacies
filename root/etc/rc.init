#!/bin/rc
#
mount -o remount,rw /
mount -a

mkdir /dev/pts
mkdir /dev/shm

mdevd-coldplug
mdevd &

hostname `{cat /etc/hostname}
ip link set lo up
dmesg >/var/log/dmesg.log

/etc/rc.local

exec perpboot /etc/perp

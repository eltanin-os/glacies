[vars]
name=perp
version=2.07
license=CUSTOM
description=A persistent process supervisor and service managment framework for un*x.

[mirrors]
git://github.com/eadwardus/perp.git

[mdeps]
libc

[build]
CFLAGS=($CFLAGS $CPPFLAGS)
portsys_mk_meth make
portsys_mk_exec

[install]
portsys_mk_exec install
